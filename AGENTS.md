# Résumé updates

- `resume/resume.tex` is the source for the academic résumé.
- Whenever the résumé changes, rebuild the PDF and update both `resume.pdf` and `resume/resume.pdf` together.
- Verify the two PDF files are byte-for-byte identical before finishing. Never update only one copy.
